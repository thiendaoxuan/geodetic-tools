# Geodetic Tools

A browser-only geodetic coordinate converter. Get your device GPS location and view it in WGS84, ISO 6709 (DMS), and ICRS (RA/Dec) formats.

## Usage

```bash
npm run dev      # Start dev server
npm run build    # Production build
npm run deploy   # Build + publish to GitHub Pages
```
